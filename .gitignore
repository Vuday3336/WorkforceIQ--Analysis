# secrets
.env
*.pem

# python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# os
Thumbs.db
.DS_Store

# power bi local artefacts
*.pbix.bak
# the whole .pbi folder is Desktop's per-machine state: the data cache, editor
# settings and local settings. None of it belongs in the repo, and cache.abf in
# particular pins the model's compatibility level and cached data source.
powerbi/**/.pbi/
